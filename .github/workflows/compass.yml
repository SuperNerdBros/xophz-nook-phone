name: Compass
on:
  push:
    branches: [main]
  schedule:
    - cron: "59 23 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  pipeline:
    uses: HalloftheGods/.github/.github/workflows/master.yml@main
